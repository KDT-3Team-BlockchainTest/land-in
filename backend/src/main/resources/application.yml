spring:
  application:
    name: backend

  profiles:
    active: local

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      "[hibernate.format_sql]": true

server:
  port: 8080

logging:
  level:
    "[com.landin.backend]": DEBUG
